// import { fromJS } from 'immutable';
// import { selectInSmileRegistrationPageDomain } from '../selectors';

describe('selectInSmileRegistrationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
