// import { fromJS } from 'immutable';
// import { selectApplicationFormPageDomain } from '../selectors';

describe('selectApplicationFormPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
